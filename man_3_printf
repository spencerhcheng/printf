.TH man 2 "22 March 2017" "0.01" "Spencer and Willis' printf function"
.SH NAME
.B printf - format and print data
Our goal was to replicate the C library function to send formatted output to
stdout.
.SH DESCRIPTION
`%c` - prints char to standard out

`%s` - prints string to standard out

`%d` - prints decimal to standard out

`%i` - prints integer to standard out

`%r` - prints reversed string to standard out

`%b` - prints binary conversion of integer or decimal to standard out

.SH OPTIONS
.B Does not take any options.
.SH ISSUES TO FIX
Multiple bugs - will update this list
.SH AUTHORS
Spencer Cheng, Willis Lau
